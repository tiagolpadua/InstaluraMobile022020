import React from 'react';

export const FotosContext = React.createContext();

export const FotosProvider = FotosContext.Provider;
export const FotosConsumer = FotosContext.Consumer;

export default FotosContext;
